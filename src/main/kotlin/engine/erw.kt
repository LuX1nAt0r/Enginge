package engine

class erw {
    fun test() {
        if (true) {
            val p = IntArray(0)
            val x = 0
            val y = 0
            val pW = 0
            var pixelcolr: Int
            val newRed = 0
            val newBlue = 0
            val newGreen = 0
            p[x + y * pW] = 255 shl 24 or (newRed shl 16) or (newGreen shl 8) or newBlue
        } else return
    }
}