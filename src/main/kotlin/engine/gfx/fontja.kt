package engine.gfx
/*
import engine.gfx.Image.width
import engine.gfx.Image.p
import engine.gfx.fontja

class fontja(path: String?) {
    private val fontImage: Image
    private val offsets: IntArray
    private val widths: IntArray

    companion object {
        val STANDARD = fontja("/fonts/standard.png")
    }

    init {
        fontImage = Image(path!!)
        offsets = IntArray(58)
        widths = IntArray(58)
        val unicode = 0
        for (i in 0 until fontImage.width) {
            if (fontImage.p[i] == -0xffff01) {
                offsets[unicode] = i
            }
        }
    }
}*/